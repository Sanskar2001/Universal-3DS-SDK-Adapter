pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()


    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://maven.juspay.in/jp-build-packages/hyper-sdk/"
        }
    }
}

rootProject.name = "ThreeDsLibrary"
include ':app'
include ':app:threeDsLibrary'
